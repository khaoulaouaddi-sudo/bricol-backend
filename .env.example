# ==== SERVER ====
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# ==== JWT / TOKENS ====
JWT_ACCESS_SECRET=change_me_access_secret
JWT_REFRESH_SECRET=change_me_refresh_secret
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=30d
COOKIE_NAME=bricol_refresh_token
COOKIE_SECURE=false
COOKIE_SAMESITE=Lax
COOKIE_DOMAIN=localhost

# ==== POSTGRES (méthode URL) ====
DATABASE_URL=postgres://postgres:postgres@localhost:5432/bricol_dev

# ==== POSTGRES (méthode variables séparées - si ton code les utilise) ====
PGHOST=localhost
PGPORT=5432
PGDATABASE=bricol_dev
PGUSER=postgres
PGPASSWORD=postgres
